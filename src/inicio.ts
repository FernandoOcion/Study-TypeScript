function sum(a: number, b: number): number {
	return a + b;
}

const result = sum(10, 2);

console.log(result);

// npm init -y -> Para começar o projeto
// npm i -D typescript -> Para instalar typescript
// node src/inicio -> Para testar o arquivo
// npx tsc --init -> Para criar o tsconfig.json
// npx tsc -> Para criar o arquivo js baseado no ts
// npm i -D tsx -> Para instalar o tsx
// -> Programar o tsx no script do package.json
// yarn dev src/inicio.ts -> Para rodar direto sem precisar tsc
